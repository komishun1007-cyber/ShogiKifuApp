<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:ShogiKifuApp.Views"
    x:Class="ShogiKifuApp.Pages.KifuDetailPage"
    Title="棋譜詳細">

    <ScrollView>
        <VerticalStackLayout Padding="12" Spacing="12">

            <!-- 対局情報 -->
            <Frame Padding="12" BorderColor="Gray" CornerRadius="8">
                <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto" ColumnDefinitions="Auto,*" RowSpacing="8" ColumnSpacing="12">
                    
                    <!-- 先手情報 -->
                    <Label Grid.Row="0" Grid.Column="0" Text="先手：" FontAttributes="Bold" VerticalOptions="Center"/>
                    <Label Grid.Row="0" Grid.Column="1" x:Name="SenteInfoLabel" Text="読込中..." VerticalOptions="Center"/>
                    
                    <!-- 後手情報 -->
                    <Label Grid.Row="1" Grid.Column="0" Text="後手：" FontAttributes="Bold" VerticalOptions="Center"/>
                    <Label Grid.Row="1" Grid.Column="1" x:Name="GoteInfoLabel" Text="読込中..." VerticalOptions="Center"/>
                    
                    <!-- 対局日時 -->
                    <Label Grid.Row="2" Grid.Column="0" Text="対局日時：" FontAttributes="Bold" VerticalOptions="Center"/>
                    <Label Grid.Row="2" Grid.Column="1" x:Name="StartDateLabel" Text="読込中..." VerticalOptions="Center"/>
                    
                    <!-- 棋戦名 -->
                    <Label Grid.Row="3" Grid.Column="0" Text="棋戦：" FontAttributes="Bold" VerticalOptions="Center"/>
                    <Label Grid.Row="3" Grid.Column="1" x:Name="TournamentLabel" Text="-" VerticalOptions="Center"/>
                    
                    <!-- 持ち時間 -->
                    <Label Grid.Row="4" Grid.Column="0" Text="持ち時間：" FontAttributes="Bold" VerticalOptions="Center"/>
                    <Label Grid.Row="4" Grid.Column="1" x:Name="TimeControlLabel" Text="-" VerticalOptions="Center"/>
                    
                    <!-- 先手戦法 -->
                    <Label Grid.Row="5" Grid.Column="0" Text="▲戦法：" FontAttributes="Bold" VerticalOptions="Center"/>
                    <Label Grid.Row="5" Grid.Column="1" x:Name="SenteStrategyLabel" Text="-" VerticalOptions="Center"/>
                    
                    <!-- 後手戦法 -->
                    <Label Grid.Row="6" Grid.Column="0" Text="△戦法：" FontAttributes="Bold" VerticalOptions="Center"/>
                    <Label Grid.Row="6" Grid.Column="1" x:Name="GoteStrategyLabel" Text="-" VerticalOptions="Center"/>
                    
                    <!-- 結果 -->
                    <Label Grid.Row="7" Grid.Column="0" Text="結果：" FontAttributes="Bold" VerticalOptions="Center"/>
                    <Label Grid.Row="7" Grid.Column="1" x:Name="ResultLabel" Text="読込中..." VerticalOptions="Center"/>
                    
                </Grid>
            </Frame>

            <!-- 将棋盤と再生ボタン -->
            <views:BoardView x:Name="BoardView"/>

            <!-- 現在の手の手筋表示 -->
            <Frame x:Name="TesujisFrame" Padding="8" BorderColor="Orange" CornerRadius="8" IsVisible="False">
                <VerticalStackLayout Spacing="4">
                    <Label Text="📌 この手の手筋" FontSize="12" FontAttributes="Bold" TextColor="DarkOrange"/>
                    <Label x:Name="CurrentTesujisLabel" Text="" FontSize="14" LineBreakMode="WordWrap"/>
                </VerticalStackLayout>
            </Frame>

            <HorizontalStackLayout Spacing="12" HorizontalOptions="Center">
                <Button Text="削除" Clicked="OnDeleteClicked" BackgroundColor="Tomato" TextColor="White"/>
            </HorizontalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>